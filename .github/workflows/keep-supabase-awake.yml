name: Keep Supabase Awake

on:
  schedule:
    # Cada 12 horas (minuto 0)
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl health/db
        run: |
          curl -sS -m 20 -L "https://mi-api-neuromkt.onrender.com/health/db" | head -c 200